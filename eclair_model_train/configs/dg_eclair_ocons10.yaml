extends: m0_eclair_frozen_29306.yaml

method:
  name: ocons

run:
  name: dg_eclair_ocons10_seed1337
  out_dir: /scratch/m23csa510/e0_results/dg_eclair_ocons10_seed1337
  save_every_epochs: 1
  keep_epoch_snapshots: false
  keep_last_k_epoch_snapshots: 0
  best_checkpoint_model_only: true
  epoch_snapshots_model_only: true
  max_run_dir_gb: 1.0

ocons:
  enabled: true
  perturbation: active_voxel_mask
  application_probability: 1.0
  mask_fraction: 0.10
  # ECLAIR train IDs: transmission wires, distribution wires, poles, towers.
  protected_class_ids: [5, 6, 7, 8]
  protected_min_voxels: 3
  protected_min_fraction: 0.70
  seed_offset: 91000
  consistency:
    max_weight: 0.50
    warmup_epochs: 10
    temperature: 1.0
    macro_min_voxels: 8
