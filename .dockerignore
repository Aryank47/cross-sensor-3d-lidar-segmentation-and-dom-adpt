# Ignore EVERYTHING by default
**

# Allow only what the image needs
!Dockerfile
!.dockerignore
!experiment0/**

# (if you have extra small text files, allow them explicitly)
!requirements.txt
!README.md
